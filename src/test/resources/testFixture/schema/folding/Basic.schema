namespace Sample

# comment
Person(人物): EntityType
    properties:
        birth(出生): Date
            desc: "x"
    desc: [[
        Define (s:App)-[p:belongTo]->(o:`TaxOfRiskApp`/`赌博应用`) {
            Structure {
                (s)
            }
        }
    ]]
